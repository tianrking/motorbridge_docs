{
  "name": "motorbridge-docs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx mintlify dev --port 3333",
    "build": "npx mintlify export -o export.zip && unzip -o export.zip -d dist",
    "preview": "npx mintlify preview"
  },
  "devDependencies": {
    "mintlify": "^4"
  }
}
